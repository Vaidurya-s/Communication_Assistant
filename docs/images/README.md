# README images

Visual assets referenced by the project `README.md`.

| File | Shows | In README? |
|------|-------|------------|
| `overlay-demo.png`  | Hero — the panel drafting a reply on a thread | ✅ under the intro |
| `overlay-panel.png` | Panel close-up (suggestion + strategy + memory card) | ✅ Features section |
| `overlay-empty.png` | The empty / initial state | spare |
| `overlay-shorter.png` | "Shorter" rewrite result | spare |
| `overlay-longer.png`  | "Longer" rewrite result | spare |
| `overlay-saved.png`   | A memory note after saving | spare |

All six are **clean recreations with fictional data** ("Alex Morgan / Vertex
Robotics") that use the extension's real styles — so they show the product
accurately without exposing any real conversation, name, or face. The four
"spare" images are available if you want to expand the README later.

They are generated from `explanation/mockups/linkedin-demo.html` (a local,
gitignored tool). Regeneration commands are in
`explanation/media-shot-list.md`.

If you ever add real screenshots, redact every real name, face, and API key
first — these images are public.
